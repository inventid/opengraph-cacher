{
  "name": "opengraph-cacher",
  "version": "2.6.2",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dist": "yarn bin && babel src --out-dir dist",
    "test": "yarn test:local && yarn test:remote",
    "test:local": "ENV=test mocha --require babel-register test/index.js && yarn lint && yarn dist",
    "test:remote": "ENV=test mocha --require babel-register --slow=2000 test/knownDomains.js",
    "lint": "eslint src/"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "camo-url": "^0.2.0",
    "elasticsearch": "^13.3.1",
    "es6-promisify": "^5.0.0",
    "express": "^4.16.2",
    "ip": "^1.1.5",
    "moment-timezone": "^0.5.14",
    "open-graph-scraper": "^3.1.3"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.0",
    "babel-plugin-transform-object-rest-spread": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "eslint": "^4.12.1",
    "mocha": "^4.0.1",
    "mocha.parallel": "^0.15.6"
  }
}
